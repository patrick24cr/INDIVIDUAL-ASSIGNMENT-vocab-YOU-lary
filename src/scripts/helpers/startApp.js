const startApp = (user) => {
  // domBuilder();
  // domEvents();
  // navBar();
  // navigationEvents();
  // formEvents();
  // logoutButton();
  // do something for the dom

};

export default startApp;
