import domBuilder from '../components/domBuilder';

const startApp = (user) => {
  domBuilder(user);
  // domEvents();
  // navigationEvents();
  // formEvents();
  // logoutButton();
  // do something for the dom
};

export default startApp;
